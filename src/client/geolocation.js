// // Experiment: Getting geolocation

export default async function geolocate(setLatitude, setLongitude) {
  const options = {
    enableHighAccuracy: true,
    timeout: 5000,
    maximumAge: 0,
  };

  //TODO: Refactor everything into this module
}
